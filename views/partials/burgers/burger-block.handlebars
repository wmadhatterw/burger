<li>
	{{name}}

	<button class="change-munch" data-id="{{id}}" data-newmunch="{{munch}}">
		{{#if munch}}Eat Burger!{{else}}Add Burger to Menu!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">X</button>
</li>
