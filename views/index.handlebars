
<h1>Burgers!</h1>

<div class="container back">

  <div class ="row back">
    <div class ="col-sm">
      <h2>Burgers On The Menu!</h2>

      <ul>
        {{#each burgers}}
          {{#unless munchy}}
            {{> burgers/burger-block munch=true}}
          {{/unless}}
        {{/each}}
      </ul>
    </div>

    <div class ="col-sm">
      <h2>Add a Burger!</h2>

        <form class="create-form">

          <div class="form-group" id = "bName">
            <label for="burger">Burger Name:</label>
            <input type="text" id="burger" name="name">
          </div>

          <div class="form-group" id = "munchBox">
            <label for="munched">Eat Burger?</label><br>
            <input type="radio" name="munchy" value="1" checked> Eat this Burger!<br>
            <input type="radio" name="munchy" value="0"> Put On Menu!
          </div>

          <button type="submit">Add burger</button>
        </form>
    </div>

    <div class ="col-sm">
      <h2>Burgers that have been eaten!</h2>

      <ul>
        {{#each burgers}}
          {{#if munchy}}
            {{> burgers/burger-block munch=false}}
          {{/if}}
        {{/each}}
      </ul>
  </div>

  
</div>